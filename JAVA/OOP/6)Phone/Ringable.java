public interface Ringable {                                                     // creates an interface named, 'Ringable'
    public String ring();                                                       // requires that the method 'ring' is used in any file that uses this interface
    public String unlock();                                                     // requires that the method 'unlock' is used in any file that uses this interface
}
